import React from "react";
import "./CryptoCards.css";

const cards = [
  {
    title: "Bitcoin",
    desc: "BTC is the original cryptocurrency, enabling decentralized money.",
  },
  {
    title: "Ethereum",
    desc: "ETH powers smart contracts and the DeFi ecosystem.",
  },
  {
    title: "Solana",
    desc: "SOL brings fast transactions and low fees to the blockchain world.",
  },
];

export default function CryptoCards() {
  return (
    <div className="crypto-container">
      {cards.map((card) => (
        <div className="crypto-card" key={card.title}>
          <div className="crypto-title">{card.title}</div>
          <div className="crypto-desc">{card.desc}</div>
        </div>
      ))}
    </div>
  );
}